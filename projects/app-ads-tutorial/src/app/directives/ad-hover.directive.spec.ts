import { AdHoverDirective } from './ad-hover.directive';

describe('AdHoverDirective', () => {
  it('should create an instance', () => {
    const directive = new AdHoverDirective();
    expect(directive).toBeTruthy();
  });
});
